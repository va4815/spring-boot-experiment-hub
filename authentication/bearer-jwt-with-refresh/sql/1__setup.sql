CREATE TABLE role
(
    id   INTEGER GENERATED BY DEFAULT AS IDENTITY,
    code VARCHAR(100) NOT NULL,
    name VARCHAR(100) NOT NULL,

    CONSTRAINT pk_role PRIMARY KEY (id),
    CONSTRAINT uk_role_code UNIQUE (code)
);

CREATE TABLE user_account
(
    id       INTEGER GENERATED BY DEFAULT AS IDENTITY,
    username VARCHAR(255) NOT NULL,
    password VARCHAR(255) NOT NULL,
    role_id  INTEGER      NOT NULL,

    CONSTRAINT pk_user_account PRIMARY KEY (id),
    CONSTRAINT uk_user_account_username UNIQUE (username),
    CONSTRAINT fk_user_account_role FOREIGN KEY (role_id) REFERENCES role (id)
);

INSERT INTO role (code, name)
VALUES ('SHOP_MANAGER', 'Shop Manager'),
       ('STAFF', 'Staff');

-- password = 'password'
INSERT INTO user_account (username, password, role_id)
SELECT 'admin',
       '$2a$12$TVJ1mfVyVE/bqH/zILKPE.nYXl0uY.XpcX66zNUYNv5KnGlUjUrb.',
       id
FROM role
WHERE code = 'SHOP_MANAGER';
